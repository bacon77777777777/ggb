/**
 * 卡包比例 —— 商品頁輪播與撕開封口演出共用的唯一來源
 *
 * 老闆 2026-08-29 指定統一。先前兩邊各寫各的：輪播照實體卡包的
 * 62 × 116 mm（1 : 1.871），撕包卻寫死 `h = w * 1.7`。同一張商品圖在輪播被
 * 置中裁切、在撕包被縱向拉伸，兩邊長得不一樣，老闆做圖也不知道該照哪個。
 *
 * 統一到 62 × 116 而不是 1.7：那是**實體卡包的真實尺寸**，不是隨手定的數字；
 * 而且輪播那邊 3D 幾何、站立高度、輪播槽位、陰影全部從它推導，動它的代價
 * 遠大於動撕包（撕包只有卡包本身與封條高度兩處在用）。
 *
 * ⚠️ 要改比例就改這裡，不要在任何一邊寫死 —— 會長歪的就是這種各寫一份的數字。
 */

/** 實體卡包尺寸（mm） */
export const PACK_MM_W = 62;
export const PACK_MM_H = 116;

/** 高 ÷ 寬 ≈ 1.871 */
export const PACK_RATIO = PACK_MM_H / PACK_MM_W;
