{
  "ignoreCommand": "{ git cat-file -e \"$VERCEL_GIT_PREVIOUS_SHA\" 2>/dev/null || git fetch --quiet --depth=1 origin \"$VERCEL_GIT_PREVIOUS_SHA\" 2>/dev/null; } && git diff --quiet \"$VERCEL_GIT_PREVIOUS_SHA\" HEAD -- . || exit 1"
}
