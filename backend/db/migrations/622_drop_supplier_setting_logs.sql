-- 622: 撤掉 supplier_setting_logs（老闆 2026-08-25）
--
-- 621 為了「廠商設定要有完整操作紀錄」開了這張表，並在廠商設定頁做了一個
-- 「變更紀錄」頁籤。老闆看過之後指定：那個頁籤不需要，紀錄統一看
-- 「系統設定 → 操作記錄」就好。
--
-- 既然沒有任何地方讀它，留著就是死 schema —— 之後看到這張表的人會以為
-- 有東西在用。改成把變更明細（誰、何時、哪一格從多少改成多少）寫進
-- action_logs.detail，操作記錄頁已加對應的中文呈現。
--
-- 表內 0 筆資料（621 建立當天就撤，沒有任何寫入）。

DROP TABLE IF EXISTS public.supplier_setting_logs;
